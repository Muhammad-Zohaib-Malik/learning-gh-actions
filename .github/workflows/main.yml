name: My First Workflow
on: push

jobs:
    my-first-job:
        runs-on: ubuntu-latest
        steps:
            - name: List all files
              run: ls -la
            - name: Print Working Directory
              run: pwd
            - name: Checkout repo
              uses: actions/checkout@v6
            - name: List all files
              run: ls -la
